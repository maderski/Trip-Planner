{
  "name": "trip-planner",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "firebase:deploy": "npm run build && npx firebase-tools deploy",
    "firebase:emulators": "npx firebase-tools emulators:start"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "firebase": "^12.10.0",
    "leaflet": "^1.9.4"
  }
}
